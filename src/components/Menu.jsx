import { AppBar, Box, Toolbar } from "@mui/material";

const Menu = () => {
  return (
    <Box
      style={{
        width: "200px",
      }}
    >
      <Toolbar>MENU</Toolbar>
    </Box>
  );
};

export default Menu;
